plugin: gcp_compute
projects:
  - file-transfer-sched-uat
auth_kind: serviceaccount
cache: false
service_account_file: ansible-file-transfer-sched-uat-db32633292a9.json
keyed_groups:
  - key: labels
    prefix: label
  - key: zone
    prefix: zone
  - key: (tags.items|list)
    prefix: tag
groups:
  gke          : "'gke' in name"
compose:
  # set the ansible_host variable to connect with the private IP address without changing the hostname
  ansible_host: name